raid
hq-srv

apt install mdadm -y

fdisk /dev/sdb
Сначала вводим g, чтобы создать раздел, затем вводим n и прокликиваем enter. Для выхода и сохранения вводим w.

Всё тоже самое проделываем с дисками sdc и sdd.

mdadm --create --verbose /dev/md0 -l 0 -n 3 /dev/sdb1 /dev/sdc1 /dev/sdd1 
mkfs.ext4 /dev/md0
mkdir /raid
mount /dev/md0 /raid

nfs
hq-srv

apt install nfs-kernel-server -y

mkdir /raid/nfs 
chmod -R 777 /raid/nfs

nano /etc/exports
добавляем строку 
/raid/nfs  192.168.200.0/28(rw,sync,no_root_squash,sudtree_check)

exportfs -a

hq-cli

apt install nfs-common
mkdir /mnt/nfs
mount -t nfs 192.168.100.2:/raid/nfs /mnt/nfs

nano /etc/fstab
добавляем строку снизу
192.168.100.2:/raid/nfs  /mnt/nfs  nfs  auto  0  0
